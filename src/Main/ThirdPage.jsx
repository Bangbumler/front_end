import React from "react";
import styled from "styled-components";

const PageContainer = styled.div`
    height: 100vh;
    background: linear-gradient(180deg, #C19DFF 0%, #F7C3FF 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    position: relative;
`;

const ThirdPage = () => {
    return (
        <PageContainer>
        </PageContainer>
    );
};

export default ThirdPage;
